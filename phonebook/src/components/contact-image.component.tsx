import React from 'react';

export const ContactImage: React.FC<HTMLImageElement> = (props) => {
	const { className, ...rest } = props;
	return (
		<></>
	);
};
// <img
// 	className={`h-12 w-12 rounded-full ${className}`}
// 	{...rest}
// 	alt=""
// />
